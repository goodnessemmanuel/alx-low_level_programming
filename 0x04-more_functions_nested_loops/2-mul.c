#include "main.h"
#include <stdio.h>

/**
 * mul - Test for digit character.
 * @a : first input
 * @b : second input
 * Return: Always a multiply by b.
 */
int mul(int a, int b)
{
	return (a * b);
}
